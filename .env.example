BREVO_API_KEY=""
BREVO_SENDER_NAME=""
BREVO_SENDER_EMAIL=""
TOMTOM_API_KEY=""

# IPMS integration (upcoming/ongoing road projects feed) — coordinate real
# values with the IPMS teammate. IPMS_BASE_URL is still localhost on their
# side during dev, so this only resolves once IPMS is deployed somewhere
# this app can reach. ROAD_MONITORING_API_KEY must be the exact same string
# as ROAD_MONITORING_API_KEY in IPMS's own .env — never invent one.
IPMS_BASE_URL=""
ROAD_MONITORING_API_KEY=""